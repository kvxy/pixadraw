package ui.terminal.menus;

// List of menus that can be switched to (STAY means stay on current menu)
public enum MenuType {
    STAY,
    COLLECTION_MENU,
    CREATE_MENU,
    EDITOR_MENU,
    LOAD_FILE_MENU,
    SAVE_FILE_MENU
}
